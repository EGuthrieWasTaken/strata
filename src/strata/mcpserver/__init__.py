"""The local MCP server (openspec:mcp-server).

A fourth thin presentation layer alongside the CLI (`strata.cli.main`) and
the web UI (`strata.web.routes`); see `strata.mcpserver.server` for the
tool set.
"""
